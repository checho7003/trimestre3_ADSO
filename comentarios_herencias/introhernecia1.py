def suma (a,b): #Se crea una funcion llamda suma con dos parametros (a,b)
    return a+b #retorna la sumatori de los dos parametros (a+b)

def suma(a,b,c): #Se cra una funcion llamada suma con tres parametros (a,b,c) 
    return a+b+c #Retorna la sumatoria de los tres parametros (a,b,c)

print(suma(1,2,3)) #Imprime la segunda funcion llamada 'suma' con el rsultado de los tres argumentos (1,2,3) el cual es 6